apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-for-app
  namespace: assignment
    #  annotations:
    #    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: 98.70.208.196.nip.io
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: two-tier-app-service
            port:
              number: 80
