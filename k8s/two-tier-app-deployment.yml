apiVersion: apps/v1
kind: Deployment
metadata:
  name: two-tier-app
  labels:
    app: two-tier-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: two-tier-app
  template:
    metadata:
      labels:
        app: two-tier-app
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: two-tier-app
          image: sahil4010/assignment:c8c29845dd08e72c1cafe0d381e4e5c5fdbeafaa
          env:
            - name: MYSQL_HOST
              value: mysql          
            - name: MYSQL_PASSWORD
              value: "admin"
            - name: MYSQL_USER
              value: "root"
            - name: MYSQL_DB
              value: "mydb"
          ports:
            - containerPort: 5000
          imagePullPolicy: Always
